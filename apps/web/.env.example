# API プロバイダの切替: mock | local | gas
# 未指定の場合は local (LocalStorage) がデフォルト。
# VITE_GAS_ENDPOINT が指定されていれば自動で gas 扱い。
VITE_API_PROVIDER=local

# gas を使う場合のみ設定。GAS Web App のデプロイURL。
# VITE_GAS_ENDPOINT=https://script.google.com/macros/s/xxx/exec
